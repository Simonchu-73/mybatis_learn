<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"></transactionManager>
            <!-- 不用properties标签是因为之前的标签是mybaitis框架底层配置的properties标签解析这里我们没有mybatis框架而是底层自己写-->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis?characterEncoding=utf8&amp;useUnicode=true&amp;serverTimezone=GMT%2B8&amp;useSSL=false&amp;allowPublicKeyRetrieval=true"/>
                <property name="username" value="root"/>
                <property name="password" value="080418"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="com/xzit/mapper/StudentMapper.xml"></mapper>
    </mappers>
</configuration>